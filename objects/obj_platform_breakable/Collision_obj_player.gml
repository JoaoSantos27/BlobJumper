/// @description Insert description here
// You can write your code in this editor
// Destroy the platform after the player touches it
if(other.vspeed > 0) {
alarm[0] = 60; // Set an alarm to trigger in 30 frames (half a second at 60 FPS)
}
